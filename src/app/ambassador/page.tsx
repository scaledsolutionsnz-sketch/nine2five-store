import AmbassadorForm from './ambassador-form';

export const metadata = {
  title: 'Ambassador Application — Nine2Five',
  description: 'Apply to become a Nine2Five brand ambassador.',
};

export default function AmbassadorPage() {
  return <AmbassadorForm />;
}
